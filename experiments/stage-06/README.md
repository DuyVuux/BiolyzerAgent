# Stage 06 Evidence Experiment

> NOT PRODUCTION CODE

Run:

```bash
python experiments/stage-06/run_benchmark.py --repo-root .
python -m unittest discover -s experiments/stage-06/tests -v
```

This experiment validates identity, provenance, claim-ledger and frozen-bundle mechanics with synthetic evidence.
