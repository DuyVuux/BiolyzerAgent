# Stage 07 Safety Harness Experiment

> NOT PRODUCTION CODE

Run:

```bash
python experiments/stage-07/run_benchmark.py --repo-root .
python -m unittest discover -s experiments/stage-07/tests -v
```

The experiment does not call an LLM.

It validates deterministic safety-contract mechanics against synthetic model candidates.
