# Stage 04 Experimental Harness

> NOT PRODUCTION CODE

Run:

```bash
python experiments/stage-04/run_benchmark.py --repo-root .
```

Tests:

```bash
python -m unittest discover -s experiments/stage-04/tests -v
```

Outputs:

```text
experiments/stage-04/results/
```
