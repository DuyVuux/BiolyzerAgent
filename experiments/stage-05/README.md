# Stage 05 Longitudinal Experiment

> NOT PRODUCTION CODE

Run:

```bash
python experiments/stage-05/run_benchmark.py --repo-root .
python -m unittest discover -s experiments/stage-05/tests -v
```

The experiment intentionally does not use a database or runtime framework.
