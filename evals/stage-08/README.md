# Stage 08 Evaluation Harness

> NOT PRODUCTION MODEL APPROVAL

Requires Stage 07 files to already exist in the repository.

Run:

```bash
python -m unittest discover -s evals/stage-08/tests -v
python evals/stage-08/run_evaluation.py --repo-root .
```
